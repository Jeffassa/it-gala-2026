# Frontend env vars (Vite)
# En dev : laisse vide, Vite proxie /api → localhost:8000
# En prod (Render) : URL publique du backend, suffixée par /api/v1
VITE_API_URL=https://it-gala-api.onrender.com/api/v1
